services:
  php:
    build: .
    volumes:
      - ./app:/var/www/html
    environment:
      - DATABASE_URL=mysql://user:pass@db:3306/isms_db
    depends_on:
      - db

  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - ./app:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php


  db:
    image: mysql:9.6.0
    environment:
      MYSQL_DATABASE: isms_db
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: user
      MYSQL_PASSWORD: pass
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
